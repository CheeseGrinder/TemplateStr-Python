from setuptools import find_packages, setup

setup(
    name='TemplateStrPython',
    packages=find_packages(),
    version='0.0.0',
    description='TemplateStr allows to add variable, function, condition and switch in a string',
    author='Xenxia',
    license='MIT',
)